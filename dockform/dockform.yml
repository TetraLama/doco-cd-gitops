# Dockform Configuration Template

docker:
  context: default
  identifier: networks

stacks: {}

networks:
  auth-net:
    driver: bridge

  dozzle-net:
    driver: bridge

  glance-net:
    driver: bridge

  immich-net:
    driver: bridge

  kavita-net:
    driver: bridge

  monitoring-net:
    driver: bridge
  
  smart-home-net:
    driver: bridge

filesets: {}
