# Dockform Configuration Template

docker:
  context: default
  identifier: networks

stacks: {}

networks:
  monitoring-net:
    driver: bridge

  auth-net:
    driver: bridge
  
  smart-home-net:
    driver: bridge

  glance-net:
    driver: bridge

filesets: {}
