# Dockform Configuration Template

docker:
  context: default
  identifier: apps

sops:
  age:
    key_file: ${AGE_KEY_FILE}

stacks:
  komga:
    root: apps/komga
    files:
      - docker-compose.yml
    env-file: []
  
  jellyfin:
    root: apps/jellyfin
    files:
      - docker-compose.yml
    env-file: []

networks:
  auth-net:
    driver: bridge

  dozzle-net:
    driver: bridge

  glance-net:
    driver: bridge

  immich-net:
    driver: bridge

  jellyfin-net:
    driver: bridge

  kavita-net:
    driver: bridge
  
  komga-net:
    driver: bridge

  monitoring-net:
    driver: bridge
  
  smart-home-net:
    driver: bridge

filesets: {}
